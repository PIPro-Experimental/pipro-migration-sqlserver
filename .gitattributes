# Normalise line endings: LF in the repo, native on checkout. Keeps the SQL
# and markdown LF; forces CRLF for the Windows script files so they run cleanly.
* text=auto eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
